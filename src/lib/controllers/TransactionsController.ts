const getAllTransactions = (householdId: number) => {

}

const getTransactionsByBudget = (budgetId: number) => {

}

// perhaps set it up so the endpoint has a number of parameters that determine it's output?
